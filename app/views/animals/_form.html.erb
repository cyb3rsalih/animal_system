<div class="form-border">
<%= nested_form_for (@animal, multipart: true) do |f| %>
    <% if @animal.errors.any? %>
    <div id="error_explanation">
        <h2><%= pluralize(@animal.errors.count, "error") %> prohibited this animal from being saved:</h2>

        <ul>
        <% @animal.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
        <% end %>
        </ul>
    </div>
    <% end %>
    <div class="form-group row ">
        <%= label_tag "Küpe No" %>
        <%= f.number_field :earring_no, class:"form-control", placeholder:"Küpe Numarasını Girin" %>
        <small id="KupeHelp" class="form-text text-muted">Hayvan'ın küpe numarası girilmesi zorunludur.</small>
    </div>

    <div class="form-group row">
        <label for="exampleInputKupeNo1">Adı</label>
            <%= f.text_field :name, class:"form-control", placeholder:"Hayvan'ın Adı" %>
    </div>

    <div class="form-group row">
        <label for="inputState">Türü</label><br/>
        <%= f.select :animal_type, Animal.animal_types.keys.map { |w| [w.humanize, w]}, class:"form-control", id:"inputType" %>
    </div>

    <div class="form-group row">
        <label>Durum</label>
        <%= f.text_field :state, class:"form-control", placeholder:"Hayvan'ın Durumu(nereye satıldı, neden öldü vb)" %>
    </div>

    <div class="form-group row">
        <label>Resmi</label>
        <%= f.file_field :image, id: :photo_image, placeholder:"Hayvan'ın Resmi", class: "form-control-file" %>
    </div>

    <div class="form-check row">
        <%= label_tag "Cinsiyeti" %><br/>
            <%= f.radio_button :gender, "Dişi", class: "form-check-input" %><%= label_tag "Dişi" %> <br/>
            <%= f.radio_button :gender, "Erkek", class: "form-check-input" %><%= label_tag "Erkek" %>
    </div>


    <div class="form-group row">
        <label>Doğum Tarihi</label>
        <%= f.date_field :birthdate, class:"form-control", placeholder:"Hayvan'ın Doğum Tarihi" %>
    </div>

    <div class="form-group row">
        <label>Ana Küpe No</label>
        <%= f.number_field :mother_earring_no, class:"form-control", placeholder:"Hayvan'ın Annesi'nin Küpe Numarası" %>
    </div>

    <div class="form-group row">
        <label>Annesi'nin Resmi</label>
        <%= f.file_field :mother_image, id: :photo_image, placeholder:"Hayvan'ın Annesi'nin Resmi", class: "form-control-file" %>
    </div>

    <div class="form-group row">
        <label>Koğuştuğu Tarihi</label>
        <%= f.date_field :mating_date, class:"form-control", placeholder:"Hayvan'ın Koğuştuğu Tarihi" %>
    </div>

    <div class="form-group row">
        <p><%= f.link_to_add "Dana Ekle", :calves, class: "btn btn-light" %></p>
        <br/>
        <%= f.fields_for :calves do |cf| %>
            <%= cf.file_field :image %>
            <%= cf.link_to_remove "Sil" %>
        <% end %>

    </div>

        <%= f.submit "Kaydet", class: "btn btn-success" %>
        <%= link_to 'Geri', animals_path, class:"btn btn-info" %>
    <% end %>
</div>